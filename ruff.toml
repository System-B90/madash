extend-exclude = [".agents", ".agent"]

# Lints under ruff's own defaults for the version pinned in
# scripts/requirements.txt. That pin is what keeps the result stable: ruff's
# default rule set moves between minors (0.16 grew it from 59 rules to 413), so
# bump it deliberately and fix what the new version flags in the same change.
